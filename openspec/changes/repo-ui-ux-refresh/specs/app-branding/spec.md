## ADDED Requirements

### Requirement: Centralized application branding
The UI SHALL source the application name and icon from a single configuration location and reuse them wherever the application identity is displayed.

#### Scenario: Sidebar header shows configured branding
- **WHEN** the sidebar renders
- **THEN** it uses the configured application name and icon
